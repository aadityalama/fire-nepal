/**
 * Clearance for FireNepalMainBottomNav card + outer padding (75px + 18px),
 * excluding safe-area which is added via env(safe-area-inset-bottom).
 * Nav is `lg:hidden`, so overlays only need this offset below the lg breakpoint.
 */
export const FIRE_NEPAL_MAIN_BOTTOM_NAV_CLEARANCE = "5.75rem";
